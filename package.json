{
  "name": "bqml-experiment-gate",
  "version": "1.0.0",
  "description": "A Leakage-Safe BigQuery ML Experiment Gate for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
